# Triadichrome 要件

## 目的

Triadichromeは、予算データファイルを開いて編集するChrome拡張機能である。

現行業務では、テーブル形式の明細を入力し、予算と実績を扱う。

このアプリは、明細入力を直感的にし、総原価表と展開表を軽量かつインタラクティブにすることを目指す。

## 利用者が行う操作

Chrome拡張機能のアイコンを操作すると、アプリは新しいタブで開く。

未読込の入口画面の中央には、次の要素だけを表示する。

- アプリ名「Triadichrome」
- 現行バージョン
- 予算データファイルを開くボタン

予算データを開いた直後の最初の画面は、施策ごとの変化を見る展開表とする。

利用者は、開いた予算データを編集できる。

編集内容は、編集のたびに、開いている元ファイルへ自動保存する。

利用者は、別名保存も利用できる。

## 扱うデータと表

当面、開くデータファイルには一つの予算と、その予算に対応する実績を保存する。

利用者は、テーブル形式の明細を入力する。

総原価表、展開表、明細は、同じ予算データを異なる軸で表す。

三つの表が同じ予算データを異なる軸で表す関係を、ここでは**三面等価**と呼ぶ。

三表の間に上下関係はない。

三表の関係に「正本と単なる出力」という区別は設けない。

入力操作の単位は施策であり、入力インターフェースは施策を起点とする。

入力操作の起点は、三表のデータ表現における三面等価の関係とは別に扱う。

入力や編集を含む主要操作は、原則としてドラッグ＆ドロップで行う。

各表の表現は、次のとおりである。

- **総原価表**：勘定科目ごとの金額を縦に並べる表
- **展開表**：施策ごとに前年からの変化を、売上額と利益額で縦に並べる表
- **明細**：施策ごと、勘定科目ごとの金額を持ち、同じ内容を売上額と利益額としても持つ表

三表はいずれも、年月ごとのデータを持つ。

総原価表と展開表は、軽量かつインタラクティブに扱えるようにする。

## 初期版の出力

初期版では、Excel形式とCSV形式の出力を提供する。

### Excel形式

利用者は、総原価表、展開表、明細から一つ以上を選択する。

選択した表は、それぞれ別のシートとして一つのファイルに出力する。

### CSV形式

CSVは、絞り込みの有無に関係なく、すべての明細だけを一つのファイルに出力する。

## 未決事項

次の事項は、現時点では確定していない。

- 予算データファイルの内部保存形式
- 計算規則
- 展開表を最初に表示すること以外の、ファイルを開いた後の画面の詳細
- Excel出力で数式をどのように扱うか
- ドラッグ＆ドロップで扱う操作対象とドロップ先
- 対象利用者の具体像と利用環境
